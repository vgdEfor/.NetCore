import { BsTabOrderDirective } from './bs-tab-order.directive';

describe('BsTabOrderDirective', () => {
  it('should create an instance', () => {
    const directive = new BsTabOrderDirective();
    expect(directive).toBeTruthy();
  });
});
