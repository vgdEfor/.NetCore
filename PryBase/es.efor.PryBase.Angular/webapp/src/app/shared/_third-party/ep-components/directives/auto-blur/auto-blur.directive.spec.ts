import { AutoBlurDirective } from './auto-blur.directive';

describe('AutoBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoBlurDirective();
    expect(directive).toBeTruthy();
  });
});
