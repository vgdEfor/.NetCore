import { FormatFromObjectPipe } from './format-from-object.pipe';

describe('FormatFromObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFromObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
