import { DtBodyDataPipe } from './dt-body-data.pipe';

describe('DtBodyDataPipe', () => {
  it('create an instance', () => {
    const pipe = new DtBodyDataPipe();
    expect(pipe).toBeTruthy();
  });
});
